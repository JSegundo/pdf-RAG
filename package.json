{
  "scripts": {
    "start": "docker compose up",
    "dev": "docker compose -f docker-compose.dev.yml up --watch",
    "build": "docker compose build",
    "stop": "docker compose down",
    "logs": "docker compose logs -f"
  }
}
